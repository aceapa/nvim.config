# nvim.config
Lua configuration for neovim.
## Requirements
Neovim >= 0.11
```bash
nvim --version
```
## Instalation
```bash
git clone --depth 1 https://github.com/aceapa/aceapa.git ~/.config/nvim
```